# 工作日志 2026-03-28

---
### [Saturday AM]
**问题：** 提交 Strings.js 动态语言读取修复到 develop 并验证 CI
**解决：** Strings.getKey() 原先在构造时锁定语言，MRPP 通过 URL 参数设置语言在构造后才生效。修改为动态从 config 读取语言，带 fallback 链（动态语言 → 初始语言 → 英语）。推送后 CI 通过。
**涉及文件：** three.js/editor/js/Strings.js
---

---
### [Saturday AM]
**问题：** 隐藏编辑器右上角的"自动保存"checkbox 和 "r183" 版本号文字
**解决：** `hideAutosaveCheckbox` 原先只搜索 checkbox label 隐藏，r183 的 UIBoolean DOM 结构变了导致没匹配到。改为直接隐藏 `.menu.right` 下的所有子元素，同时覆盖 autosave 和版本号。
**涉及文件：** plugin/patches/SidebarPatches.ts
---

---
### [Saturday PM]
**问题：** 为 tripo3d-ai-plugin 创建 spec 需求文档，后发现方向有误，全部还原
**解决：** 创建了 tripo3d-ai-plugin 和 userdata-keyvalue-ui 两个 spec 目录及需求文档，但用户确认这不是正确的项目上下文，执行 `git checkout` 还原工作日志，`rm -rf` 删除两个新建 spec 目录，恢复到上次 git 提交状态。
**涉及文件：** 无（已还原）
---

---
### [Saturday PM]
**问题：** 用户询问项目有哪些值得改良重构的地方
**解决：** 通过 context-gatherer 全面分析项目结构，结合已有 specs 和 steering 文件，梳理出 7 个改良点：external-modules.d.ts 重复声明、tsconfig baseUrl 技术债、缺少根目录 ESLint、遗留 devDependencies、CI/CD 文档缺失、MrppEditor 类型逃生舱、patch 注入缺集成测试。
**涉及文件：** 无
---

---
### [Saturday PM]
**问题：** 为项目最需要改良的地方创建 spec
**解决：** 确认优先处理「patch 注入集成测试」，采用 Requirements-First 工作流创建需求文档。文档包含 11 个需求，核心策略是纯静态文件系统分析（读取源码做字符串/正则匹配），覆盖导出函数名验证、bootstrap 调用顺序、Editor API 存在性、信号名称、导入路径合法性、幂等性守卫等关键风险点。
**涉及文件：** .kiro/specs/patch-injection-tests/.config.kiro、.kiro/specs/patch-injection-tests/requirements.md
---

---
### [当前时间 ~session end]
**问题：** 自定义数据（userData）字段用纯文本 textarea 显示 JSON，需要改为只读可视化树形展示
**解决：** 在 `Sidebar.ObjectExt.ts` 末尾新增 `injectUserDataJsonViewer` 函数，隐藏原 textarea，插入带折叠/展开、语法着色的树形 JSON viewer；同时在 `DeferredUIPatches.ts` 中补上了之前从未被调用的 `injectSidebarObjectExtensions`，并一并调用新函数。更新了 export-names-preserved 测试以包含新导出名。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`, `plugin/utils/DeferredUIPatches.ts`, `three.js/editor/test/properties/export-names-preserved.test.js`
---

---
### [session end]
**问题：** 修改后页面没有变化
**解决：** 项目使用 `plugin-dist/` 编译产物，TS 源码修改后需要执行 `npm run build` 才能生效。运行构建后问题解决。
**涉及文件：** 无（仅执行构建）
---

---
### [session end - debug]
**问题：** userData JSON viewer 修改后页面仍无变化
**解决：** 发现两个问题：1) 需要 `npm run build` 才能生效（已解决）；2) `injectUserDataJsonViewer` 依赖 `tryPatchSidebar` 时传入的 `objectContentDom`，但该时机 `#objectTab` 尚未被 `addTab` 创建，导致 container 为 null。改为让函数只接收 `editor`，在每次 `objectSelected` 时从 `document` 全局搜索 userData row，彻底绕开时机问题。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`, `plugin/utils/DeferredUIPatches.ts`
---

---
### [session end]
**问题：** 用户提到"数据默认是收起来的，等用户自行打开"，但未指明具体组件
**解决：** 无实质性变更，用户未提供足够上下文（哪个文件/哪个 UI 组件），对话结束前未能推进。
**涉及文件：** 无
---

---
### [session end - style fix]
**问题：** JSON viewer 在白天模式下深色背景突兀，且 key-value 会换行
**解决：** 将 viewer 样式改为亮色默认（VS Code light 风格），通过 `@media (prefers-color-scheme: dark)` 覆盖暗色配色；给每行加 `div.jv-line` + `white-space: nowrap` 修复换行问题。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** JSON viewer 默认展开，希望默认折叠让用户手动打开
**解决：** 将 array/object 节点的 children 初始 class 改为 `jv-children jv-collapsed`，toggle 初始文字改为 `▸`，点击后正常切换。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** 将本次改动推送到 develop 分支
**解决：** `git add -A && git commit && git push origin develop`，共 7 个文件变更。
**涉及文件：** 无（仅 git 操作）
---

---
### [session end]
**问题：** 为 patch-injection-tests spec 创建设计文档
**解决：** 基于已有需求文档生成 design.md，设计了 4 个测试文件（按关注点分组）、8 个 property-based tests、纯静态文件系统分析策略，与现有 vitest + fast-check 框架保持一致。
**涉及文件：** `.kiro/specs/patch-injection-tests/design.md`
---

---
### [session end]
**问题：** 为 patch-injection-tests spec 创建 tasks.md
**解决：** 无实质性变更，用户发送了 tasks 创建请求但会话结束前未能完成。
**涉及文件：** 无
---

---
### [session end]
**问题：** 为 patch-injection-tests spec 创建 tasks.md
**解决：** 生成了 tasks.md，包含 5 个顶层任务（4 个测试文件 + 1 个验证任务），覆盖 8 个属性测试和全部 11 个需求，属性测试子任务标记为可选。
**涉及文件：** `.kiro/specs/patch-injection-tests/tasks.md`
---

---
### [session end]
**问题：** 执行 patch-injection-tests spec 的全部任务
**解决：** 依次创建 4 个测试文件（patch-exports、patch-language-signals、patch-dependencies、patch-idempotency），覆盖 8 个属性测试（P1–P8）和 10 个单元测试。执行过程中发现两处需要适配实际代码的地方：ui.three.js 使用 ES6 class 而非 prototype 模式；resolveImportPath 需要额外支持 `.d.ts` 候选路径以处理 `mrpp.d.ts` 类型导入。最终 `npx vitest --run` 全部 114 个测试通过，无回归。
**涉及文件：** `three.js/editor/test/properties/patch-exports.test.js`, `three.js/editor/test/properties/patch-language-signals.test.js`, `three.js/editor/test/properties/patch-dependencies.test.js`, `three.js/editor/test/properties/patch-idempotency.test.js`
---

---
### [session end]
**问题：** 将 patch-injection-tests 的 4 个新测试文件推送到 develop 并等待 CI 通过
**解决：** `git add -A && git commit && git push origin develop`，CI Pipeline 在约 1 分钟内完成，Test (34s) 和 Build (45s) 均绿，Docker 镜像正常推送。
**涉及文件：** 无（仅 git 操作）
---

---
### [session end]
**问题：** 为消除 plugin/ 目录下所有 TypeScript `any` 类型创建 spec 需求文档
**解决：** 先扫描全项目 `any` 分布，分类整理为 7 类消除策略，采用 Requirements-First 工作流创建需求文档，共 8 个需求，覆盖 Command 泛型化、NodeData/ResourceData 接口定义、UIPanel 类型替换、BridgeHandlers payload 类型化、MrppEditor 接口收紧、索引签名评估及属性测试防回归。明确排除 external-modules.d.ts 通配符声明中的 any（JS 互操作边界，必要保留）。
**涉及文件：** `.kiro/specs/eliminate-any-types/requirements.md`, `.kiro/specs/eliminate-any-types/.config.kiro`
---

---
### [session end]
**问题：** 将侧边栏"自定义数据"JSON 只读树形展示改为可编辑，`type` 和 `resource` 字段保持只读
**解决：** 重构 `buildNode` 函数，增加 `parentObj`、`key`、`onChanged` 参数；叶子节点按类型渲染为 `<input type="text/number">` 或 `<select>`；`READONLY_KEYS = ['type', 'resource']` 的字段渲染为半透明只读样式；值变更后直接写回 `object.userData` 并 dispatch `objectChanged` 信号。补充了 input/select 的亮色/暗色样式。构建通过无新增错误。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** 将自定义数据展示从树形 JSON viewer 改为扁平表格，按数据类型提供不同编辑控件
**解决：** 完全重写 `injectUserDataJsonViewer`，改用 `<table>` 渲染顶层 key-value；string→text input、number→number input、boolean→select、object/array→只读紧凑 JSON 文本（hover 显示完整内容）；`type`/`resource` 保持只读。同步清理了旧的树形样式，补充新表格样式，亮色/暗色均适配。构建通过无新增错误。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** 将 `meta_id` 字段加入自定义数据表格的只读列表
**解决：** 在 `READONLY_KEYS` Set 中追加 `'meta_id'`，一行改动，构建通过。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** 询问 `userData.draggable` 字段的用途及系统是否使用
**解决：** 搜索代码发现：`MetaFactory.ts` 在加载模块时硬编码写入 `userData.draggable = false`；`VerseLoader.ts` 序列化时将其列入 `exclude` 跳过保存；`UIThreePatches.ts` 读取的是 DOM 原生 `draggable` 属性，与 userData 无关。结论：该字段是运行时标记，不持久化，目前全部固定为 false。
**涉及文件：** 无
---

---
### [session end]
**问题：** 将 `draggable` 加入只读列表，并将所有只读字段在自定义数据表格中隐藏
**解决：** `READONLY_KEYS` 追加 `'draggable'`；`renderTable` 中对只读 key 直接 `return` 跳过，不渲染该行。现在 `type`、`resource`、`meta_id`、`draggable` 四个系统字段在 UI 上完全不可见。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.ts`
---

---
### [session end]
**问题：** Kiro 弹出 "Unable to resolve your shell environment" 错误
**解决：** 无实质性变更，这是 IDE shell 初始化超时问题，与项目代码无关，建议检查 ~/.zshrc 中的慢速初始化脚本。
**涉及文件：** 无
---
