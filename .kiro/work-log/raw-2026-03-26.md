# 工作日志 2026-03-26

---
### [约 00:00]
**问题：** 升级 three.js 到 r183 后，编辑菜单中"清空历史记录"显示为 `???`
**解决：** r183 移除了 `menubar/edit/clear_history` 这个 i18n key，而 `MenubarPatches.js` 手动添加 Clear History 时使用了 `menubar/edit/clearHistory`（驼峰命名），该 key 在 r183 的 `Strings.js` 和 `MrppStrings.js` 中均不存在。在 `MrppStrings.js` 的 5 个语言段（en-us, zh-cn, ja-jp, zh-tw, th-th）中补充了 `menubar/edit/clearHistory` 的翻译。
**涉及文件：** `plugin/i18n/MrppStrings.js`
---

---
### [约 00:15]
**问题：** 升级 r183 后，截图侧边栏面板中"显示网格"标签显示为 `???`
**解决：** 原因是 `Sidebar.Screenshot.js` 使用了旧版 key `sidebar/settings/viewport/grid`，该 key 在 r183 中已被移除（r183 重构为 `viewport/controls/grid`）。改为使用 MRPP 自定义的 `sidebar/screenshot/showGrid`，并在 `MrppStrings.js` 的 5 个语言段中补充了翻译。
**涉及文件：** `plugin/ui/sidebar/Sidebar.Screenshot.js`, `plugin/i18n/MrppStrings.js`
---

---
### [约 00:30]
**问题：** r183 升级后侧边栏对象面板底部多了一个"导出JSON"按钮，MRPP 模式不需要
**解决：** 在 `SidebarPatches.js` 的 `applySidebarPropertiesPatches` 中，获取到 objectContentDom 后通过 `querySelector('button')` 找到该按钮并设置 `display: none`，不侵入 three.js 原始代码。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 00:45]
**问题：** 首次隐藏"导出JSON"按钮的 patch 未生效，按钮仍然显示
**解决：** 原因是 `objectContentDom` 在 patch 初始化时可能还未完全渲染，单次 `querySelector('button')` 找不到。改为提取 `hideExportJsonButton()` 函数，在初始化时调用一次，并在每次 `objectSelected` 信号触发重建 objectTab 后再次调用，确保按钮始终被隐藏。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 01:00]
**问题：** 底部动画播放面板是否需要隐藏
**解决：** 尝试在 verse-bootstrap.js 中增强 tryHideAnimation 逻辑以拦截 r183 Animation.js 的 objectSelected 信号重新显示面板的行为，但用户决定保留动画面板显示，已还原修改。无实质性变更。
**涉及文件：** 无（已还原）
---

---
### [约 01:15]
**问题：** r183 底部动画面板应该只在选中有动画的对象时显示，未选中或选中无动画对象时应隐藏
**解决：** r183 的 Animation.js 初始化时无条件显示面板，且 objectSelected 回调只处理有动画的情况不处理隐藏。在两个 bootstrap 的 tryHideAnimation 中增加 objectSelected 监听，当选中对象无动画时强制隐藏面板和 resizer 并 dispatch false。
**涉及文件：** `plugin/bootstrap/verse-bootstrap.js`, `plugin/bootstrap/meta-bootstrap.js`
---

---
### [约 01:30]
**问题：** 未选中有动画对象时，底部动画面板的白色 resizer 条仍然显示
**解决：** r183 的 Animation.js 的 `update()` 会被 `objectAdded`/`objectRemoved` 信号触发，无条件 dispatch panelHeight 导致 resizer 重新显示。在两个 bootstrap 中增加 `animationPanelChanged` 信号拦截器，当 height 不为 false 时检查当前选中对象是否有动画，无动画则强制隐藏面板和 resizer。
**涉及文件：** `plugin/bootstrap/verse-bootstrap.js`, `plugin/bootstrap/meta-bootstrap.js`
---

---
### [约 01:45]
**问题：** 通过 inline style 设置 `display: none` 无法可靠隐藏动画面板的 resizer 白色条
**解决：** r183 的 AnimationResizer 通过信号监听器不断用 inline style 覆盖 `display` 值，导致 JS 层面的 `style.display = 'none'` 被后续信号处理覆盖。改用 CSS class `.mrpp-animation-hidden { display: none !important; }` 配合 `classList.toggle()` 来控制显隐，`!important` 确保优先级高于任何 inline style。
**涉及文件：** `plugin/bootstrap/verse-bootstrap.js`, `plugin/bootstrap/meta-bootstrap.js`
---

---
### [约 02:00]
**问题：** CSS class + !important 方案仍未能隐藏 animation-resizer 白色条
**解决：** 根本原因是 `#animation` 和 `#animation-resizer` 在 HTML 中按顺序添加到 DOM，MutationObserver 在 `#animation` 添加时触发 tryHideAnimation 并设置 `animationHidden = true`，此时 `querySelectorAll('#animation-resizer')` 返回空 NodeList（resizer 还没创建）。后续 resizer 添加时因 `animationHidden` 已为 true 直接跳过。修复为 `setAnimationHiddenClass` 每次调用时用 `getElementById` 重新查询而非依赖缓存的 NodeList。
**涉及文件：** `plugin/bootstrap/verse-bootstrap.js`, `plugin/bootstrap/meta-bootstrap.js`
---

---
### [约 02:15]
**问题：** 提交本次 UI 修复并推送到远程，检查 CI 状态，以及确认未提交文件的内容
**解决：** 将 6 个 plugin 文件的修改提交并推送到 `feature/editor-plugin-separation` 分支。未提交的文件分为：plugin/ 下约 11 个之前升级适配的改动、three.js/ 下 r183 升级的大量差异（~678 修改 + ~1247 删除 + 大量新增）、以及 .kiro/ 工作文件和截图等。
**涉及文件：** 无新修改，仅 git 操作
---

---
### [约 02:30]
**问题：** 提交剩余的 plugin 层 r183 适配改动
**解决：** 将 9 个 plugin 文件（Factory, MetaFactory, MetaLoader, VerseLoader, EditorPatches, UIThreePatches, ViewportPatches, Menubar.Screenshot, Sidebar.ObjectExt）的 r183 适配改动提交并推送。
**涉及文件：** `plugin/mrpp/Factory.js`, `plugin/mrpp/MetaFactory.js`, `plugin/mrpp/MetaLoader.js`, `plugin/mrpp/VerseLoader.js`, `plugin/patches/EditorPatches.js`, `plugin/patches/UIThreePatches.js`, `plugin/patches/ViewportPatches.js`, `plugin/ui/menubar/Menubar.Screenshot.js`, `plugin/ui/sidebar/Sidebar.ObjectExt.js`
---

---
### [约 02:40]
**问题：** 确认 three.js/ 目录下的 r183 升级变更是否也需要提交
**解决：** 检查了 .gitignore 配置，确认 three.js/ 目录没有被排除。结论是需要全部提交，否则线上部署时 three.js 仍为旧版，plugin 层的 r183 适配代码会出错。建议分一个单独 commit 提交 three.js/ 的 r183 升级。
**涉及文件：** 无
---

---
### [约 02:45]
**问题：** 提交 three.js/ 目录下的 r183 完整升级差异并推送
**解决：** 将 three.js/ 目录下 3071 个文件的 r183 升级变更（70 万行新增、42 万行删除）全部提交并推送到 `feature/editor-plugin-separation` 分支。CI 已自动触发。
**涉及文件：** `three.js/` 目录下 3071 个文件（r183 完整升级）
---

---
### [约 03:00]
**问题：** 提交剩余的 .kiro 工作文件并触发 CI
**解决：** 提交了 specs、work-log、steering 等 11 个 .kiro 工作文件并推送，CI 已自动触发。
**涉及文件：** `.kiro/specs/`, `.kiro/work-log/`, `.kiro/steering/patch-injection-patterns.md`
---

---
### [约 03:15]
**问题：** 侧边栏层级面板中搜索框和类型下拉框的布局与旧版不一致（新版上下排列，旧版左右并排）
**解决：** 将 `injectOutlinerSearchUI` 中原来的两个 `UIRow`（searchRow + filterRow）合并为一个 flex 布局的行，搜索框 flex:1 占剩余空间，下拉框固定 120px，去掉了"类型"标签文字。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 03:30]
**问题：** verse 编辑器的层级面板不需要类型过滤下拉框，只需要搜索框
**解决：** 在 `injectOutlinerSearchUI` 中根据 `editor.type` 判断，meta 编辑器显示搜索框+类型下拉框并排，verse 编辑器只显示搜索框占满整行。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 03:45]
**问题：** r183 升级后 meta 编辑器 outliner 列表的自定义类型图标丢失（全部变成蓝色圆点），以及 verse 编辑器的拼图图标丢失
**解决：** r183 的 `getObjectType` 只返回 three.js 原生类型，不识别 MRPP 自定义类型。新增 `injectOutlinerCustomIcons` 函数，通过 MutationObserver 监听 outliner 变化，根据 `userData.type` 替换 type span 的 CSS class，并注入旧版的自定义图标 CSS（不同类型用不同符号和颜色）。verse 编辑器则在 bootstrap 中注入 CSS 把 Object3D 圆点替换为拼图 SVG 图标。
**涉及文件：** `plugin/patches/SidebarPatches.js`, `plugin/bootstrap/verse-bootstrap.js`
---

---
### [约 04:15]
**问题：** injectOutlinerCustomIcons 未生效，outliner 图标仍为默认圆点
**解决：** MRPP 的自定义类型（Polygen、Point、Text 等）是设置在 `object.type` 上而非 `object.userData.type`。原代码只检查了 `userData.type` 所以永远匹配不到。修改为先检查 `object.type`，并用 nativeTypes 列表排除 three.js 原生类型，只对 MRPP 自定义类型替换图标 CSS class。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 04:30]
**问题：** 修改 object.type 检查后图标仍未生效，需要调试确认对象的实际 type 值
**解决：** 在 `replaceTypeIcons` 中添加 console.log 调试输出，打印每个对象的 `object.type` 和 `object.userData.type`，等待用户刷新页面后查看控制台日志以确认实际值。待下次会话继续调试。
**涉及文件：** `plugin/patches/SidebarPatches.js`（临时调试日志）
---

---
### [约 04:45]
**问题：** 调试日志确认 replaceTypeIcons 正确识别了 MRPP 类型（Entity、Polygen），但图标仍未变化
**解决：** 根本原因是 CSS 选择器优先级不够。r183 的 `#outliner .type:after { content: '●'; }` 优先级高于注入的 `#outliner .Polygen:after`。将选择器改为 `#outliner .type.Polygen:after`（双 class 选择器），优先级高于 `#outliner .type:after`。同时移除了调试 console.log。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 05:00]
**问题：** 图标已经生效但与旧版样式不完全一致，需要截图对比确认差异
**解决：** 等待用户提供新旧版对比截图以确认具体哪些图标样式需要调整。待下次会话继续。
**涉及文件：** 无
---

---
### [约 05:05]
**问题：** 确认 outliner 自定义图标已与旧版一致
**解决：** 用户确认图标样式已正确恢复，无需进一步修改。
**涉及文件：** 无
---

---
### [约 05:15]
**问题：** r183 升级后 outliner 中 MRPP 自定义类型对象（如 Polygen）可以展开显示内部 three.js 子节点（Armature、Mesh 等），旧版这些节点是锁定不可展开的
**解决：** 在 `injectOutlinerCustomIcons` 的 `replaceTypeIcons` 中，对 MRPP 自定义类型对象隐藏 opener 按钮（`display: none`），并遍历后续 option 元素，根据 paddingLeft 判断子节点层级，隐藏属于该对象子树的 three.js 内部节点。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [约 05:30]
**问题：** 属性面板中阴影、视锥体裁剪、渲染次序行需要隐藏，自动保存需要默认为否
**解决：** 原来用 `style.display = 'none'` 隐藏这些行，但 r183 的 objectSelected 处理器会在之后重新设置 display 覆盖掉。改用 CSS class `.mrpp-hidden { display: none !important; }` 确保不被覆盖。自动保存在 `EditorPatches.js` 中设置 `config.setKey('autosave', false)`。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.js`, `plugin/patches/EditorPatches.js`
---

---
### [约 05:45]
**问题：** CSS class + !important 方案仍未能隐藏阴影/视锥体裁剪/渲染次序行
**解决：** 添加调试日志检查 `allRows.length` 和 `findRowByLabel` 是否能找到 shadow 行。怀疑 `allRows` 在初始化时获取的静态 NodeList 不包含 r183 后续动态添加的行，或者 label 文本匹配有问题。待用户查看控制台日志后继续调试。
**涉及文件：** `plugin/ui/sidebar/Sidebar.ObjectExt.js`（临时调试日志）
---

---
### [约 18:00]
**问题：** 隐藏编辑器侧边栏中的"阴影"、"视锥体裁剪"、"渲染次序"属性行，以及菜单栏右上角的"自动保存"复选框
**解决：** 在 SidebarPatches.js 中新增 `hideObjectPropertyRows`（通过 objectSelected 信号 + Promise 微任务延迟，在 SidebarObject 的 updateRows 之后隐藏指定行）和 `hideAutosaveCheckbox` 两个函数。在 meta-bootstrap.js 和 verse-bootstrap.js 中导入并调用。过程中踩坑：用 shell `cp` 截断文件后 IDE 缓存不同步，导致 fsAppend/strReplace 反复失败产生重复内容，最终用 Python 文件写入方式解决。
**涉及文件：** plugin/patches/SidebarPatches.js, plugin/bootstrap/meta-bootstrap.js, plugin/bootstrap/verse-bootstrap.js
**踩坑记录：** 写大文件不要用 cat/cp/shell 重定向，会和 IDE 文件系统冲突，应使用 Python 的 open/write 方式
---

---
### [约 22:00]
**问题：** 删除 .kiro/specs/system-admin-plugin/ 目录，切换到最新 develop 分支，并创建 TypeScript 迁移特性分支
**解决：** 删除了 system-admin-plugin spec 目录，提交了 .gitignore 和工作日志的修改，切换到 develop 并拉取最新代码（已是最新），然后从 develop 创建了 feature/plugin-typescript-migration 分支。
**涉及文件：** 无
---

---
### [约 23:00]
**问题：** JS → TS 迁移前需要做哪些准备工作，并扫描是否还有可分离的 three.js 侵入代码
**解决：** 创建了 `js-to-ts-migration-prep` spec 的需求文档，涵盖 8 个需求：构造函数转 class、JSDoc 类型注释、全局变量显式声明、Object3D 扩展属性标注、tsconfig 配置、测试配置更新、no-typescript 测试反转、现有测试保持通过。扫描确认 three.js 中仅剩 Strings.js（5 处）和 Commands.js（1 处）有 MRPP 侵入，均为最小侵入形式，不建议进一步分离。
**涉及文件：** `.kiro/specs/js-to-ts-migration-prep/requirements.md`（新建）
---

---
### [约 23:30]
**问题：** 为 js-to-ts-migration-prep spec 创建设计文档和任务列表，以及检查未提交代码
**解决：** 尝试为 js-to-ts-migration-prep spec 继续推进设计文档和任务列表的创建，但因会话中多次重复请求导致进展有限。最后检查了 git status，确认未提交文件包括：js-to-ts-migration-prep spec 目录、工作日志、plugin/admin/、screenshots/、three.js.backup/、three.js-r183.zip 等。建议将备份文件和 node_modules 加入 .gitignore。
**涉及文件：** 无实质性文件修改
---

---
### [约 23:45]
**问题：** 删除 plugin/admin/ 目录，并将 screenshots/、three.js-r183.zip、three.js.backup/、node_modules/ 加入 .gitignore
**解决：** 删除了 plugin/admin/ 目录，在 .gitignore 中追加了 4 条忽略规则。清理后未提交文件只剩 .gitignore、工作日志和 js-to-ts-migration-prep spec。
**涉及文件：** `.gitignore`, `plugin/admin/`（已删除）
---
