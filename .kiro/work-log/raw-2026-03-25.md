# 工作日志 2026-03-25

---
### [约 00:10]
**问题：** 完成上一轮遗留的 git commit 操作
**解决：** 确认 94 个文件已全部 staged，执行 git commit 提交到 feature/editor-plugin-separation 分支（commit 14e8d5b）。提交信息涵盖 MRPP 业务代码迁移到 plugin/、修改标记、Commands.js 路径修复、4 个属性测试等。
**涉及文件：** 无新修改，仅提交已暂存的变更
---

---
### [约 00:20]
**问题：** 用户计划升级 three.js，要求做前期重构以降低升级难度
**解决：** 创建了新 spec（threejs-upgrade-prep-refactor），完成需求文档。分析 12 个 editor 文件共 77 处 MRPP 侵入式修改，制定 17 个需求项，核心策略是通过 monkey-patch + 信号监听 + DOM 操作将修改抽离到 Bootstrap_Module 和 Plugin_Layer，目标从 77 处降至 20 处以内。用户尚未确认需求文档。
**涉及文件：** .kiro/specs/threejs-upgrade-prep-refactor/requirements.md, .kiro/specs/threejs-upgrade-prep-refactor/.config.kiro
---

---
### [约 01:00]
**问题：** 用户确认 threejs-upgrade-prep-refactor 需求文档，要求继续推进技术设计
**解决：** 创建了设计文档，核心方案是在 plugin/patches/ 下创建 patch 模块，通过 monkey-patch + DOM 注入将 12 个 editor 文件中 77 处侵入式修改外部化。定义了 9 个新模块（EditorPatches、SidebarPatches、MenubarPatches、LoaderPatches、ViewportPatches、UIThreePatches、Menubar.MrppAdd、Menubar.MrppEdit、Sidebar.ObjectExt）和 8 个正确性属性（4 个新增 + 4 个现有）。用户尚未确认设计文档。
**涉及文件：** .kiro/specs/threejs-upgrade-prep-refactor/design.md
---

---
### [约 02:00]
**问题：** 用户确认设计文档，要求生成 threejs-upgrade-prep-refactor 的任务列表
**解决：** 生成了 tasks.md，共 12 个顶层任务按依赖顺序排列：EditorPatches 核心模块 → Loader/Viewport/UIThree patches → UI 扩展模块（MrppAdd/MrppEdit/ObjectExt）→ Sidebar/Menubar patches → Bootstrap 更新 → 恢复 10 个原版文件 → 属性测试 → 验证。Spec 三件套（需求、设计、任务）全部就绪。
**涉及文件：** .kiro/specs/threejs-upgrade-prep-refactor/tasks.md
---

---
### [约当前时间]
**问题：** 确认 CI 能生成哪几个版本的 Docker 镜像
**解决：** 查看了 `.github/workflows/build.yml` 和 `Dockerfile`，梳理出 CI 根据 publish/develop/main 分支分别生成 latest、publish、develop、main 四个 tag，推送到腾讯云镜像仓库。
**涉及文件：** 无
---

---
### [约 03:00]
**问题：** 执行 threejs-upgrade-prep-refactor spec 的所有任务（run all tasks）
**解决：** 完成了 task 2.3（UIThreePatches 验证）、checkpoint 3、task 4.1-4.3（创建 Menubar.MrppAdd.js、Menubar.MrppEdit.js、Sidebar.ObjectExt.js）、task 5.1-5.2（创建 SidebarPatches.js、MenubarPatches.js）、checkpoint 6、task 7.1-7.2（更新 meta-bootstrap.js 和 verse-bootstrap.js 调用所有 patches）、task 8.1-8.9（恢复 Editor.js、Sidebar.js、Menubar.js、Sidebar.Properties.js、Menubar.Add.js、Menubar.Edit.js、Loader.js、Viewport.js、ui.three.js 为 r140 原版）。UIThreePatches.js 额外增加了 UIOutliner prototype 多选 API 的 monkey-patch（因 ui.three.js 恢复原版后多选功能需外部注入）。剩余：task 8.10（Sidebar.Object.js 恢复）、checkpoint 9、task 11（运行测试）、checkpoint 12。会话因长度限制中断。
**涉及文件：** plugin/patches/SidebarPatches.js, plugin/patches/MenubarPatches.js, plugin/ui/menubar/Menubar.MrppAdd.js, plugin/ui/menubar/Menubar.MrppEdit.js, plugin/ui/sidebar/Sidebar.ObjectExt.js, plugin/bootstrap/meta-bootstrap.js, plugin/bootstrap/verse-bootstrap.js, plugin/patches/UIThreePatches.js, three.js/editor/js/Editor.js, three.js/editor/js/Sidebar.js, three.js/editor/js/Menubar.js, three.js/editor/js/Sidebar.Properties.js, three.js/editor/js/Menubar.Add.js, three.js/editor/js/Menubar.Edit.js, three.js/editor/js/Loader.js, three.js/editor/js/Viewport.js, three.js/editor/js/libs/ui.three.js
---

---
### [03:20]
**问题：** 继续执行 threejs-upgrade-prep-refactor spec 的剩余任务（Task 8.10 ~ Task 12）
**解决：** 完成了 Task 8.10（恢复 Sidebar.Object.js 为 r140 原版，移除约 1000 行 MRPP 代码），通过 Checkpoint 9 验证所有 10 个恢复文件均为 0 MRPP 标记，运行 Task 11 和 Task 12 确认全部 13 个属性测试通过。跳过了可选的 Task 10.1/10.2。至此 spec 所有必需任务全部完成。
**涉及文件：** `three.js/editor/js/Sidebar.Object.js`
---

---
### [07:07]
**问题：** 重构后 Add 菜单显示了原版几何体菜单项和 MRPP 菜单项叠加，对菜单的修改无效
**解决：** 根因是 `MenubarPatches.js` 中 `injectMrppAddMenu` 注入 MRPP 菜单项前没有先清空原版 `.options` 容器的子元素。在注入前添加了 `while (firstChild) removeChild` 清空逻辑。同时确认了 CI 构建配置：publish 分支生成 latest+publish tag，develop 分支生成 develop tag，main 分支生成 main tag。
**涉及文件：** `plugin/patches/MenubarPatches.js`

**踩坑经验：** 外部化侵入式修改为注入式 patch 时，需区分"替换型"和"追加型"场景。替换型（如 Add 菜单完全替换原版内容）必须在注入前清空原有 DOM 子元素；追加型（如 Edit 菜单在原版基础上增加功能）可直接 append。
---

---
### [07:10]
**问题：** Edit 菜单也存在与 Add 菜单相同的重复项问题，需要全面排查所有 patch 注入点
**解决：** 系统排查了所有 6 个 patch 模块和 3 个 UI 扩展模块的注入方式。确认 Edit 菜单有重复的 Clone 和 Delete 项（原版+MRPP 各一份），修复方式为注入前按文本匹配移除原版的 Clone 和 Delete，保留 Undo/Redo/Clear History/Center/Fix Color Maps。其余注入点（SidebarPatches、SidebarPropertiesPatches、Sidebar.ObjectExt、Screenshot/Scene/Goto）均无问题。
**涉及文件：** `plugin/patches/MenubarPatches.js`

**建议：** 考虑在 `.kiro/steering/` 中新增 patch 编写指南，记录注入时"完全替换型（先清空）vs 部分替换型（移除特定项）vs 追加型（直接 append）"的判断规则。
---

---
### [07:12]
**问题：** 选中对象后属性面板（属性/动画/组件 tab）不显示
**解决：** 恢复 `Sidebar.js` 为原版时，`scene.add(propertiesPanel)` 被误判为 MRPP 修改而删除，但它实际上是 r140 原版就有的代码（scene tab 用 flex column 布局，上面 SidebarScene 下面 SidebarProperties）。加回这行后属性面板恢复正常。
**涉及文件：** `three.js/editor/js/Sidebar.js`

**踩坑经验：** 恢复原版文件时，当 MRPP 修改和原版代码交织在一起（如同一个函数内既有原版逻辑又有 MRPP 扩展），容易把原版代码误判为 MRPP 修改而删除。恢复前应对比 r140 原始 commit 确认哪些是真正的原版代码。
---

---
### [07:14]
**问题：** Sidebar 顶部 tab 数量不对，meta 模式多了"项目"和"设置" tab
**解决：** 原版 `Sidebar.js` 总是添加 scene/project/settings 三个 tab，但 MRPP 模式下不需要 project 和 settings。在 `SidebarPatches.applySidebarPatches` 中新增步骤 2：meta/verse 模式下从 DOM 和 wrapper 数组中移除 project 和 settings tab。修复后 meta 模式显示 3 个 tab（层级/信号/截图），verse 模式显示 2 个 tab（实体/截图）。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [07:24]
**问题：** 全面审查所有 patch 注入点，整理已修复的问题并检查是否有遗漏
**解决：** 系统读取了全部 6 个 patch 文件和 3 个 UI 扩展模块，逐一审查每个注入点。发现并修复了 `SidebarPatches.js` 中 `const editorType` 重复声明的 bug（步骤 2 新增代码和步骤 3 原有代码各声明了一次，运行时会报错）。确认共 5 个问题已全部修复，其余 7 个注入点无问题。
**涉及文件：** `plugin/patches/SidebarPatches.js`
---

---
### [07:27]
**问题：** 将 plugin/ 目录中误用 console.error 作为日志输出的地方改为 console.log
**解决：** 搜索了 plugin/ 下所有 console.error 调用，区分了"调试/信息日志"和"真正的错误处理"两类。将 7 处非错误场景（用户角色日志、加载成功日志、调试数据打印等）从 console.error 改为 console.log，保留了 catch 块中的真正错误报告。
**涉及文件：** `plugin/bootstrap/verse-bootstrap.js`, `plugin/ui/menubar/Menubar.MrppAdd.js`, `plugin/mrpp/MetaFactory.js`, `plugin/mrpp/MetaLoader.js`, `plugin/ui/sidebar/Sidebar.Media.js`
---

---
### [07:30]
**问题：** Sidebar 顶部 tab（层级/信号/截图）切换一轮后就卡住无法切换
**解决：** 根因是 `wrapAsTabbedPanel` wrapper 和原版 `UITabbedPanel` 实例各自维护独立的 `selected` 状态和 `tabs/panels` 数组。原版 tab 的点击事件绑定到原版实例的 `select`，wrapper 添加的 tab 绑定到 wrapper 的 `select`，两者状态不同步。修复方式：在 wrapper 初始化时给所有已有 tab 添加 capture-phase 点击事件，调用 `wrapper.select` 并 `stopImmediatePropagation` 阻止原版事件处理器，统一由 wrapper 管理所有 tab 切换。
**涉及文件：** `plugin/bootstrap/meta-bootstrap.js`, `plugin/bootstrap/verse-bootstrap.js`

**踩坑经验：** 用 DOM wrapper 包装已有 UI 组件实例时，原版实例的事件处理器不会自动被 wrapper 接管。必须显式拦截（capture-phase + stopImmediatePropagation）或替换原版事件绑定，否则会出现双重状态管理导致的 UI 卡死。
---

---
### [07:36]
**问题：** 第二次选择元素时属性面板不显示（sidebar tab 切换卡住 + properties 面板消失）
**解决：** 两个问题。1) Sidebar tab 切换卡住：`wrapAsTabbedPanel` wrapper 和原版 `UITabbedPanel` 实例各自维护独立的 `selected` 状态，原版 tab 点击事件绑定到原版实例而非 wrapper。修复：初始化 wrapper 时给已有 tab 添加 capture-phase 点击事件拦截原版处理器。2) Properties 面板第二次不显示：`objectTabPanel.dom.firstChild` 在 `clear()`/`addTab()` 循环中被 `appendChild` 移走后变成 `null`。修复：初始化时直接保存 `objectContentDom` 引用而非每次通过 `firstChild` 间接获取。系统检查了所有 `clear()`/`addTab()` 循环，确认无其他同类问题。
**涉及文件：** `plugin/bootstrap/meta-bootstrap.js`, `plugin/bootstrap/verse-bootstrap.js`, `plugin/patches/SidebarPatches.js`
---

---
### [07:39]
**问题：** Edit 菜单仍然显示原版的"居中"和"修复颜色贴图"选项，MRPP 模式下不需要
**解决：** 之前只移除了原版 Clone/Delete，但 Center 和 Fix Color Maps 也不需要。改为找到 Clear History 后移除其后所有内容（分隔线+居中+克隆+删除+分隔线+修复颜色贴图），然后注入 MRPP 的 Clone/Replace/Delete。
**涉及文件：** `plugin/patches/MenubarPatches.js`
---

---
### [07:40]
**问题：** 让当前分支 feature/editor-plugin-separation 也能在 CI 中构建 Docker 镜像
**解决：** 在 `ci.yml` 的 push 触发分支列表中加入 `feature/editor-plugin-separation`，在 `build.yml` 的 metadata tags 中加入 `editor-plugin-separation` tag 规则。
**涉及文件：** `.github/workflows/ci.yml`, `.github/workflows/build.yml`
---

---
### [07:42]
**问题：** 推送 feature/editor-plugin-separation 分支触发 CI
**解决：** 暂存所有变更（排除 node_modules），提交 33 个文件（+7407/-3555 行），推送到远程。CI 已触发。
**涉及文件：** 无新修改，仅 git 操作
---

---
### [09:58]
**问题：** 用户反馈 CI 有修改错误，需要排查
**解决：** 检查了 ci.yml、build.yml、test.yml 三个 workflow 文件，未发现语法错误。本地测试全部通过（13/13）。等待用户提供 GitHub Actions 具体错误日志以进一步定位。
**涉及文件：** 无
---

---
### [10:00]
**问题：** CI ESLint 报错 `No files matching the pattern "js/mrpp" were found`
**解决：** lint 路径还指向旧的 `js/mrpp` 和 `js/utils`，但代码已移到 `plugin/`。更新了 `package.json` lint 命令指向 `../../plugin/`，`eslint.config.js` files 模式改为 `../../plugin/**/*.js`，同时允许 `console.log`。已推送触发 CI。
**涉及文件：** `three.js/editor/test/package.json`, `three.js/editor/test/eslint.config.js`
---

---
### [11:10]
**问题：** CI ESLint 仍然失败，files 匹配 `../../plugin/` 被 ESLint 10 忽略
**解决：** ESLint flat config 的 `files` 模式相对于 config 文件位置解析，而 CLI 目标路径相对于 cwd。两者不一致导致文件被忽略。最终方案：从项目根目录运行（`cd ../../../`），config 路径用 `three.js/editor/test/eslint.config.js`，files 模式用 `plugin/**/*.js`，pin `eslint@9` 避免 v10 兼容问题。CI 已通过。
**涉及文件：** `three.js/editor/test/package.json`, `three.js/editor/test/eslint.config.js`
---

---
### [11:15]
**问题：** 检查 develop 分支上的新修改并整理文档
**解决：** develop 分支有 1 个新提交（f307127）：MervinTT02 添加了 verse 编辑器的"实体导航菜单"功能（MenubarEntity），涉及 3 个文件。整理了合并注意事项：Menubar.js 和 Menubar.Entity.js 需要按本分支架构迁移到 plugin/ 层，Strings.js 可直接合并。
**涉及文件：** `DEVELOP_CHANGES.md`（新建）
---

---
### [11:20]
**问题：** 将 develop 分支的 MenubarEntity 功能应用到本分支
**解决：** 分析后确认不应在本分支引入新功能。本分支目标是升级 three.js 版本（代码分离），develop 的 MenubarEntity 应等本分支合并回 develop 后再按 plugin 架构适配。生成了 `DEVELOP_CHANGES.md` 作为后续合并参考。
**涉及文件：** `DEVELOP_CHANGES.md`（新建，待确认是否保留）
---
